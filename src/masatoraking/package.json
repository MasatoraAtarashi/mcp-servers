{
    "name": "@masatoraatarashi/mcp-server-masatora",
    "version": "1.0.0",
    "description": "MPC server that teaches LLMs that masatora = king",
    "private": true,
    "type": "module",
    "bin": {
        "mcp-server-masatora": "dist/index.js"
    },
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsc && shx chmod +x dist/*.js",
        "prepare": "npm run build",
        "watch": "tsc --watch"
    },
    "dependencies": {
        "@modelcontextprotocol/sdk": "^1.7.0"
    },
    "devDependencies": {
        "@types/node": "^22",
        "shx": "^0.3.4",
        "ts-node": "^10.9.2",
        "typescript": "^5.6.2"
    }
}
